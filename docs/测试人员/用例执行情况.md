# 移动端图像直方图分析系统用例执行情况

## 1. 文档说明

本文档记录测试用例的执行情况。当前记录分为两部分：

- 研发自测执行情况：已有脚本和本机命令结果，状态可判定。
- 验收测试执行情况：APK 产物已具备，部分断网测试已有用户反馈，但最终手机型号、Android 版本、截图和耗时仍需回填。

## 2. 执行状态口径

| 状态 | 含义 |
| --- | --- |
| 通过 | 已执行并达到预期 |
| 部分通过 | 有正向反馈或部分证据，但缺少完整记录 |
| 待回填 | 需要手机环境补充执行结果 |
| 不适用 | 非核心路径或当前阶段不作为必测项 |
| 未通过 | 已执行但未达到预期 |

## 3. 研发自测执行汇总

| 用例编号 | 用例名称 | 执行方式 | 执行结果 | 状态 |
| --- | --- | --- | --- | --- |
| ST-01 | 灰度公式测试 | `npm run test:histogram` | Histogram algorithm fixture passed | 通过 |
| ST-02 | 256 bin 统计测试 | `npm run test:histogram` | bins 长度和统计规则通过 fixture | 通过 |
| ST-03 | 像素总数一致性测试 | `npm run test:histogram` | Histogram main-flow fixture passed | 通过 |
| ST-04 | 归一化测试 | `npm run test:histogram` | 归一化规则通过 fixture | 通过 |
| ST-05 | `256x100` 绘制测试 | `npm run test:histogram` | 绘制尺寸和规则通过 fixture | 通过 |
| ST-06 | 主流程 fake Canvas 测试 | `npm run test:histogram` | 主流程 fixture passed | 通过 |
| ST-07 | 离线 assets 检查 | `npm run test:offline` | Offline asset checks passed | 通过 |
| ST-08 | baseline 一致性测试 | `npm run test:baseline` | Baseline histogram fixture passed | 通过 |
| ST-09 | 性能 benchmark | `npm run benchmark:histogram` | 已输出 small、mid、large 三组数据 | 通过 |
| ST-10 | 源码注释检查 | `npm run check:source-comments` | 14 个 app/src 文件已检查，核心注释比例达标 | 通过 |
| ST-11 | harness 综合校验 | `npm run harness:verify` | Harness verification passed | 通过 |

## 4. 研发自测最近一次执行记录

执行日期：2026-07-08

执行命令：

```bash
npm run harness:verify
```

执行结果摘要：

```text
Harness document check passed.
Histogram algorithm fixture passed.
Histogram main-flow fixture passed.
Offline asset checks passed.
app/src/main/assets/app.js code=1033 comments=337 ratio=0.326
app/src/main/java/com/framia/mobilehistogram/MainActivity.java code=139 comments=55 ratio=0.396
core-source-total code=1172 comments=392 ratio=0.334 required>0.333
app-src-commented-files checked=14 missing=0
Harness verification passed.
```

说明：

- Android 构建验证未在本命令中执行，脚本提示可设置 `RUN_ANDROID_BUILD_VERIFY=1` 后构建；
- APK 构建证据已在阶段 1、阶段 4、阶段 5 文档中记录；
- 当前命令主要证明文档、算法、离线和注释检查通过。

## 5. 性能 benchmark 执行记录

执行命令：

```bash
npm run benchmark:histogram
```

已有记录来自 `docs/测试人员/stage5-performance-comparison.md`：

| Case | Image size | Pixels | Comparison baseline | Current optimized | Speedup | 当前实现是否低于 300ms |
| --- | --- | ---: | ---: | ---: | ---: | --- |
| `small-demo` | 320x180 | 57,600 | 46.22 ms | 1.42 ms | 32.5x | 是 |
| `phone-mid` | 800x600 | 480,000 | 407.1 ms | 2.30 ms | 176.9x | 是 |
| `phone-large` | 1280x720 | 921,600 | 700.4 ms | 1.99 ms | 351.6x | 是 |

注意：以上是开发机 Node.js 合成数据，不替代 Android 真机测试。

## 6. APK 交付执行记录

| APK | 路径 | 包名 | 状态 |
| --- | --- | --- | --- |
| 基础阶段 APK | `dist/mobile-histogram-stage4-debug.apk` | `com.framia.mobilehistogram` | 已打包，有包信息和 SHA256 |
| 优化前 APK | `dist/mobile-histogram-baseline-coinstall-debug.apk` | `com.framia.mobilehistogram.baseline` | 已打包，用于性能对照 |
| 优化后 APK | `dist/mobile-histogram-optimized-coinstall-debug.apk` | `com.framia.mobilehistogram.optimized` | 已打包，作为正式演示版本 |

阶段 4 记录显示 APK 内包含本地 H5 assets；阶段 5 记录显示 baseline 和 optimized 可并装，入口分别为 baseline 页面和 optimized 页面。

## 7. 验收测试执行情况

| 用例编号 | 用例名称 | 当前执行情况 | 状态 | 待补证据 |
| --- | --- | --- | --- | --- |
| AT-01 | 优化版 APK 安装 | APK 已输出，最终手机安装明细未回填 | 待回填 | 手机型号、Android 版本、安装截图 |
| AT-02 | 断网启动 | 用户曾反馈第 3 阶段断网测试通过，但缺完整记录 | 部分通过 | 断网截图、设备信息 |
| AT-03 | 本地图片选择 | 代码已实现，手机完整记录待补 | 待回填 | 操作截图或录屏 |
| AT-04 | 原图预览 | 代码已实现，手机截图待补 | 待回填 | 预览截图 |
| AT-05 | 直方图生成 | 代码和脚本已验证，手机截图待补 | 待回填 | 输出页截图 |
| AT-06 | 耗时展示 | 代码已实现，真机耗时待补 | 待回填 | 图片尺寸、耗时截图 |
| AT-07 | 指标说明 | 页面已实现，截图待补 | 待回填 | 指标页截图 |
| AT-08 | 协议说明 | 页面已实现，截图待补 | 待回填 | 协议页截图 |
| AT-09 | 重新选择图片 | 代码已支持，验收记录待补 | 待回填 | 前后结果截图 |
| AT-10 | 异常取消选择 | 代码已支持，验收记录待补 | 待回填 | 操作记录 |
| AT-11 | 优化前 APK 安装 | 可并装 APK 已输出，手机截图待补 | 待回填 | 双图标截图 |
| AT-12 | 优化前后性能对比 | benchmark 已有，手机同图对比待补 | 待回填 | 两版本耗时截图 |
| AT-13 | 直方图放大 | 功能已实现，验收记录待补 | 待回填 | 放大截图 |
| AT-14 | 长按保存拼接图 | 辅助功能，非核心验收路径 | 不适用 | 如需展示再补 |
| AT-15 | 演示路径完整性 | 演示路径已设计，现场预演记录待补 | 待回填 | 预演记录 |

## 8. 问题记录

| 编号 | 问题 | 影响 | 状态 | 处理建议 |
| --- | --- | --- | --- | --- |
| BUG-01 | 真机安装验证信息缺失 | 测试报告证据不完整 | 待回填 | 补手机型号、Android 版本、安装和运行截图 |
| BUG-02 | 真机耗时表缺失 | `300ms` 目标在手机侧缺少完整记录 | 待回填 | 用同一图片记录 optimized 和 baseline 耗时 |
| BUG-03 | README 部分旧路径仍指向 `docs/测试/` | 文档索引可能误导查找 | 已知 | 本次测试文档使用真实 `docs/测试人员/` 路径，后续可统一修 README |

## 9. 执行结论

研发自测已经通过，能够证明灰度公式、256 bin、归一化、`256x100` 绘制、离线 assets 和 baseline 一致性等关键规则没有问题。

验收测试具备执行条件，但最终测试报告仍需要补充至少一组 Android 手机实测记录。没有这些回填数据时，可以说明 APK 和脚本证据已经具备，但不应把真机安装、截图和实测耗时写成完整完成。
